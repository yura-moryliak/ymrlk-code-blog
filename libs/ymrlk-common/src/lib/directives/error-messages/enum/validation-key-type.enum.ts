export enum ValidatorsKeyType {
  EMAIL = 'email',
  REQUIRED = 'required',
  MIN_LENGTH = 'minlength',
  MAX_LENGTH = 'maxlength',
  MIN = 'min',
  MAX = 'max',
  REQUIRED_TRUE = 'requiredTrue',
  PATTERN = 'pattern'
}
