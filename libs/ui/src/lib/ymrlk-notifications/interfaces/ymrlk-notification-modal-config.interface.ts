/**
 * @module EccNotificationsModule
 */

export interface YmrlkNotificationModalConfigInterface {

  /**
   * Property to check if backdrop click (closing modal by click outside) was allowed or not.
   */
  backdropDismiss?: boolean;

  /**
   * Property to checked close button availability.
   */
  closeButton?: boolean;
}
